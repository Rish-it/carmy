---
description: Run Carmy's self-contained RED-GREEN TDD protocol
argument-hint: "[bug or behavior]"
---

Load and apply the installed `carmy-tdd` skill to: $ARGUMENTS

Find the existing test harness; prove one public behavior RED; make the
smallest root-cause fix GREEN; then run relevant verification twice. Do not add
a framework or mock internals.
